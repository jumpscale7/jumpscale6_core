#!/bin/bash
curdir=`pwd`
QBASE=/opt/jumpscale

PATH=$QBASE:$QBASE/bin:$PATH

# if [ "x$IPYTHONDIR" == "x" ]; then
#   export IPYTHONDIR=$QBASE/cfg/ipython
# fi

mkdir -p $QBASE/var/pid
mkdir -p $QBASE/var/log
mkdir -p $QBASE/var/tmp

python $QBASE/utils/shell.py "$1" "$2" "$3" "$4"
