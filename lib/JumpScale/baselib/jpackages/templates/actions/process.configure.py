def main(jp):
   
    #configure the application to autostart
    
    # jp.log("set autostart $(jp.name)")

    # #example start with circus
    # cmd = 'python'
    # args2 = 'osisServerStart.py'
    # workingdir = j.system.fs.joinPaths(j.dirs.baseDir, 'apps', 'osis')
    # j.tools.startupmanager.addProcess(jp.name, cmd, args2, priority=2, workingdir=workingdir,ports=[])

    # env_vars = {'anenvvar': 9200}
    # j.tools.startupmanager.addEnv('jp.name', env_vars)
    # j.tools.startupmanager.apply()
    
    # jp.start()


    pass
