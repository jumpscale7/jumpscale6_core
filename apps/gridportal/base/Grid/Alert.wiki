@usedefaults
{{alert: guid:$$guid}}
{{timestamp}}

h3. Details
|*ECO*|${eco}|
|*Raise time*|{{div: class=jstimestamp}}${inittime}{{div}}||
|*Last time*|{{div: class=jstimestamp}}${lasttime}{{div}}|

h3. Actions

  {{actions:

- display: Accept
  action: /restmachine/system/alerts/update
  data:
   alert: '$$guid'
   state : 'ACCEPTED'
  input:
  - comment
  
- display: Assign
  action: /restmachine/system/alerts/update
  data:
   alert: '$$guid'
   state : 'ACCEPTED'
   
  input:
  - username
  
- display: Resolve
  action: /restmachine/system/alerts/update
  data:
   alert: '$$guid'
   state : 'RESOLVED'
  input:
  - comment
  
- display: Mark unresolved
  action: /restmachine/system/alerts/update
  data:
   alert: '$$guid'
   state : 'UNRESOLVED'
  input:
  - comment
    

- display: Close
  action: /restmachine/system/alerts/update
  data:
   alert: '$$guid'
   state : 'CLOSED'
  input:
  - comment

- display: Re-Open
  action: /restmachine/system/alerts/update
  data:
   alert: '$$guid'
   state : 'ALERT'
  input:
  - comment
}}

h3. History
{% if history %}||User||Comment||State||Time||{% else -%}
|NO HISTORY|
{% endif %}
{% for h in history -%}
| ${h.user} | ${h.comment} | ${h.state} | {{div: class=jstimestamp}}${h.epoch}{{div}} |
{% endfor %}